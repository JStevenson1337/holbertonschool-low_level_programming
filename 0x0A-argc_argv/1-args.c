#include "main.h"
#include<stdio.h>
/**
 * @argv: Array
 * @argc: number of arguments
 * Description: prints the number of arguments
 * Return: Exit
 *
 */
 int main(int argc, char *argv[])
 {
     int i, sum = 0;

     if(args <= 0)
     {
         for (i = 1; i < argc; i++)
         {
             printf("%d",argv)
         }
     }


 }
