#include<stdio.h>
/*
 *Write a C program that prints exactly with
 *proper grammar, but the outcome is a
 *piece of art,, followed by a new line.
*/

/**
 *  main - Entry point
 * Return:  0 returns the string
 *
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
